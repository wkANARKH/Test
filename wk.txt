1.git init;git add;git commit -m "说明信息"；
2.git status;
3.git diff;git log;
4.git checkout -- <files>   a. No execute git add;
5.git checkout -- <files>   b. execute git add but no execute git commit;
6.git rm  
7.git push 
8.git clone